name: HQF API Tests

on:
  repository_dispatch:
    types: [run-api-tests]

jobs:
  api-tests:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout tests
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Run Playwright API tests
        env:
          BRANCH_NAME: ${{ github.event.client_payload.branch }}
          PR_URL: ${{ github.event.client_payload.pr_url }}
          SOURCE_REPO: ${{ github.event.client_payload.repo }}
        run: |
          echo "Running API tests for branch: $BRANCH_NAME"
          echo "Triggered by PR: $PR_URL"
          echo "Source repository: $SOURCE_REPO"
          # example: run tests with environment var for target API
          npx playwright test --project=api --reporter=github

      - name: Upload report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
